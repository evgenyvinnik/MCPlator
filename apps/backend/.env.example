# Anthropic API Key (required for production, not needed if USE_MOCK_LLM=true)
ANTHROPIC_API_KEY=sk-ant-xxx

# Optional: Port for local dev server (default: 3001)
PORT=3001

# Optional: Use mock LLM for local testing without Anthropic API (default: false)
# Set to 'true' to test frontend + backend locally without API key
USE_MOCK_LLM=false
